# 카툰은 무엇이 될 것인가

이 프로그램이 어디로 가는지 적어 둔다. 잊어도 여기 남게 하려는 것이다.
**아직 확정된 것이 아니다.** 사장님이 정하시면 그때 고친다.

## 사장님 말씀 그대로 (2026-09-14 07:47 · box:cartoon · 짚으신 자리 「그림」)

> 이렇게 업로드된 사진을 참고하여 이옆에 추가 그림음 에이아이가 생성하여 계속 새페이지를
> 만들어가는 방식 입니다. 박스의 개념은 아직 확정하지 마세요 지금 만든 최초의 박스는
> 여기서 처음 업로드된 첫번째그림을 영감으로 마들어질 첫 카툰의 작업장 입니다.

## 알아들은 것

- **카툰은 참고 그림판이 아니다.** 만화를 만들어 가는 **작업장**이다.
- 올린 사진은 **씨앗(영감)** 이다. 그 **옆에 AI가 그림을 이어 그려** 새 페이지를 계속 만들어 간다.
- **지금 만든 첫 박스 = 첫 카툰의 작업장.** 그 안의 **첫 번째 올린 그림**이 그 카툰의 씨앗이다.
- 그러니 박스는 「폴더」가 아니라 **「카툰 한 편」** 에 가까울 수 있다. **아직 확정 아니다.**

## 그래서 지키는 것

- **박스 개념을 굳히지 않는다.**
  - `cartoon_boxes` 의 생김새(필드·쓰임)를 더 손보지 않는다.
  - 박스를 「갈래·폴더」로 굳히는 화면·글귀를 더 만들지 않는다.
  - 박스에 딸린 새 기능을 스스로 만들지 않는다.
  - 지금 있는 것(만들기 · 이름 · 아이콘 · 고르기 · 담기 · 옮기기 · 지우기)에서 **멈춘다.**
- **AI 그림 만들기는 아직 붙이지 않는다.** 시키신 적이 없다.
  어느 도구를 쓸지, 돈이 드는지도 지금 정하지 않는다.

## 지금 되는 것 (씨앗까지는 놓여 있다)

- 그림 올리기 (파이어베이스 저장소, 구글 콘솔에서 켤 것 없음)
- 박스 만들어 그 안에 담기 · 다른 박스로 옮기기
- 눌러서 크게 보기 · 원본 새 창으로 열기
- 다른 기기에서 따라오기 (파이어스토어 `cartoon_refs` · `cartoon_boxes`)
- 뒤로 물려 둔 길: 드라이브 공유 링크 붙여넣기 · 드라이브에서 고르기(구글 콘솔 켜야 함)

## 두는 곳

    artifacts/vivivic-4b7ef/public/data/cartoon_refs/{그림id}    { id, at, box, kind, url, path, name }
    artifacts/vivivic-4b7ef/public/data/cartoon_boxes/{박스id}   { id, name, icon, at }
    저장소(그림 파일)  vivivic-4b7ef.firebasestorage.app  /cartoon/{그림id}

`box` 가 비어 있으면 갈래 없음. 박스를 지워도 그림은 지워지지 않고 갈래 없음으로 돌아간다.

## 고칠 때 기억할 것

- `app/index.html` 이 **원본**, `build.sh` 가 그것을 `index.html` 로 **복사**한다.
  **둘 다 맞춰야** 다음 배포에 되돌아가지 않는다.
- `images.json` 은 `build.sh` 가 만든다. 손으로 고치지 않는다.
- 드라이브 파일은 **읽기만** 한다. 옮기지도 지우지도 않는다.
- 화면은 **폰(375px) 기준**. 마우스·넓은 화면(1280px)도 함께 본다.
- 0.5초 길게 누르기는 **바이글 손잡이**가 쓰고 있다. 거기에 다른 손짓을 겹치지 않는다.
